name: shell commands

on: [push]

jobs:
  run-shell-command: 
    runs-on : ubuntu-latest 
    steps:  
      - name: echo a string 
        run: dontecho "Hello this is testing "
      - name: multiline script 
        run: | 
          node -v
          npm -v